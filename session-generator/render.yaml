services:
  - type: web
    name: paxton-md-session-generator
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node session-generator/index.js
    # No PORT env needed — Render injects PORT automatically and
    # session-generator/index.js binds to process.env.PORT first.
